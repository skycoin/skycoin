package wallet

import (
	"testing"
)

func TestNewBip44Wallet(t *testing.T) {
	// fn := "foo.wlt"
	// tf := make(mockTxnsFinder) // TODO: setup mock TransactionsFinder
	// w, err := newWallet(fn, Options{
	// 	Type: WalletTypeBip44,
	// 	Seed: "voyage say extend find sheriff surge priority merit ignore maple cash argue",
	// }, tf)
	// require.NoError(t, err)
}
